#include <stdio.h>
 int main()
{
char s[1000];
int i,length=0,a;
printf("Enter any string: ");
gets(s);
for(i=0; i<=s; i++);
a=s/10;
length = length++
printf("Length of '%s' = %d",s,length);
return 0;
}
